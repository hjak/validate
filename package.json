{
  "name": "@hjk/validate",
  "version": "2.0.7",
  "description": "A simple schema json validation",
  "author": "hjakk",
  "license": "MIT",
  "repository": "hjakk/validate",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist && npm run build:bundle",
    "build:bundle": "babel ./src -d ./dist",
    "prepare": "npm run build",
    "version": "git add -A src",
    "postversion": "git push"
  },
  "keywords": [
    "validation",
    "schema",
    "json"
  ],
  "devDependencies": {
    "@babel/cli": "^7.5.0",
    "@babel/core": "^7.5.4",
    "@babel/preset-env": "^7.5.4",
    "rimraf": "^2.6.3"
  }
}
